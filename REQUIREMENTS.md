# 邮件集成系统 — 需求规划

> 版本 v0.2 · 状态：草案（剩余待确认见第 10 节）

## 1. 项目定位与目标

一句话：**以 macOS 桌面客户端形态，接收邮箱新邮件 → AI 提取结构化内容 → 推送到 Telegram（聊天平台）**。

形态：**GUI 桌面客户端（macOS 窗口应用）**，内置常驻后台引擎，非命令行/非服务器部署。

目标用户场景：不想频繁查邮箱的人，希望重要邮件自动以"摘要卡片"形式出现在聊天工具里，同时在桌面客户端内查看、管理、配置。

## 2. 范围

### 2.1 本期范围（In Scope）
- **GUI 桌面客户端**（macOS 窗口应用，参考 Reroot Healthcare 风格）
- 邮箱接入：**Gmail**、**QQ 邮箱**（可扩展）
- 邮件接收：新邮件检测与抓取（后台引擎常驻轮询）
- AI 提取：正文 + 元信息 → 结构化摘要（DeepSeek）
- 推送：Telegram Bot
- 客户端内：账号管理、配置、推送历史/提取结果查看
- 基础工程：配置、日志、去重、失败重试

### 2.2 暂不包含（Out of Scope）
- 邮件**发送**/回复（目录名虽为 EmailSend，但当前需求只提"收到邮件"，发送列为未来扩展）
- 多用户 SaaS 化（单用户本地客户端）
- 附件内容深度解析（本期仅识别附件存在与文件名，不 OCR/解析 PDF）
- Windows/Linux 版本（先 macOS）

## 3. 核心业务流程

```
邮件到达 → 检测(轮询) → 拉取原文 → 过滤规则(可选)
  → 去重判断 → AI 提取 → 格式化消息 → Telegram 推送 → 记录状态 → 客户端界面展示
```

## 4. 功能需求（FR）

| 编号 | 需求 | 优先级 |
|------|------|--------|
| FR-1 | 接入 QQ 邮箱：IMAP 拉取新邮件（授权码认证） | P0 |
| FR-2 | 接入 Gmail：IMAP 拉取新邮件（App Password） | P0 |
| FR-3 | 统一 MailProvider 抽象，屏蔽 Gmail/QQ 差异（收件箱、拉取、标记已读） | P0 |
| FR-4 | 新邮件检测：后台引擎周期轮询 | P0 |
| FR-5 | 去重：基于 UID / Message-ID 持久化，避免重复推送 | P0 |
| FR-6 | AI 提取：原文 → 结构化 JSON（摘要 + 待办/行动项 + 关键实体〔金额/日期/人名〕 + 分类 + 紧急度） | P0 |
| FR-7 | Telegram 推送：格式化卡片，长文本分段，附件名提示 | P0 |
| FR-8 | 过滤规则：发件人白/黑名单、关键词（可选开关） | P1 |
| FR-9 | 多账号配置：每账号独立凭证、规则、AI 提示词、推送目标(chat_id) | P1 |
| FR-10 | 失败重试与状态记录（处理中/成功/失败/跳过） | P1 |
| FR-11 | 结构化日志与可观测（处理量、失败原因） | P2 |
| FR-12 | GUI 主界面：邮件流卡片视图（发件人/主题/摘要/分类标签/紧急度/待办实体） | P0 |
| FR-13 | GUI 设置页：账号、AI(DeepSeek)、Telegram、过滤规则配置 | P0 |
| FR-14 | GUI 状态栏：同步状态、处理计数、最近推送 | P1 |

## 5. 非功能需求（NFR）

| 编号 | 需求 |
|------|------|
| NFR-1 | **幂等**：同一封邮件绝不重复推送（UID 去重 + 事务标记） |
| NFR-2 | **安全**：邮箱授权码 / API Key 加密存储（Electron `safeStorage`），不落明文 |
| NFR-3 | **可靠**：网络/API 失败可重试，退避策略，单封失败不阻塞整体 |
| NFR-4 | **可扩展**：新增邮箱类型、新增推送平台只需实现接口 |
| NFR-5 | **UI 精致度**：还原 Reroot 医疗健康风格（见第 6 节 UI 规范） |
| NFR-6 | **性能**：后台轮询不卡 UI（主进程与渲染进程分离） |

## 6. 技术选型

| 层 | 建议 | 说明 |
|----|------|------|
| 桌面框架 | **Electron**（备选 Tauri 2 / SwiftUI） | macOS 窗口应用，UI 还原度最高、生态最全 |
| 前端 | React + TypeScript + Vite + Tailwind CSS | 还原 Reroot 风格，组件化 |
| 后端引擎 | Node.js（Electron main 进程） | 与前端同栈，统一 TS；常驻后台轮询 |
| 邮件接入 | `imapflow`（IMAP 客户端协议） | QQ/Gmail 统一 IMAP，授权码/App Password |
| AI | `openai` SDK → **DeepSeek** 端点 | 国内直连、便宜，可切换 |
| Telegram | `node-telegram-bot-api` 或 fetch Bot API | 轻量 |
| 存储 | SQLite（`better-sqlite3`） | 去重表 + 推送历史 |
| 调度 | Electron 内 `node-cron` / `setInterval` | 常驻后台轮询收件箱 |
| 打包 | `electron-builder` → `.app` / `.dmg` | macOS 分发 |

> 注：原 Python 技术栈（imapclient/openai/APScheduler）在"常驻后台进程"形态下成立；转 GUI 客户端后，全 TS 栈更统一、打包更简单。若坚持 Python 后端可走 Electron + Python sidecar，但复杂度上升，不推荐。

## 7. UI 设计规范（参考 Dribbble — Reroot Healthcare App）

### 配色（取自参考 shot 官方色板）

| 角色 | 色值 | 用途 |
|------|------|------|
| 背景 | `#F5F4E2` | 米白主背景 |
| 主深色 | `#183018` | 深橄榄绿，主文字/标题/深色块 |
| 强调 | `#F07848` | 珊瑚橙，CTA/高亮/高紧急标记 |
| 辅助绿 | `#486048` | 橄榄/鼠尾草绿，次要强调/图标 |
| 辅助中性 | `#A1967E` | 卡其灰棕，次级文字/边框/分割线 |
| 中性灰 | `#9D9D9D` | 弱化文字/占位符 |

### 风格关键词
温暖自然 · 有机 · earth-tone · 橄榄绿+珊瑚橙 · 大量留白 · 大圆角卡片 · 精致排版 · 柔和专业感

### 布局
- **左侧边栏**：邮箱账号列表（Gmail/QQ 图标 + 未读计数）+ 底部设置入口
- **主视图**：邮件流卡片（发件人、主题、AI 摘要、分类标签、紧急度徽标、待办/关键实体）
- **设置页**：账号管理、AI(DeepSeek) 配置、Telegram 配置、过滤规则
- **状态栏**：同步状态、处理计数、最近推送时间

## 8. 系统架构

```
┌─────────────────────────────────────────────────┐
│              Electron 桌面应用 (macOS)             │
│  ┌──────────────────┐   ┌─────────────────────┐  │
│  │ Renderer (React) │◄─►│ Main 进程 (Node)     │  │
│  │ UI 界面/设置/历史 │IPC│  后台引擎(常驻轮询)   │  │
│  └──────────────────┘   └──────────┬──────────┘  │
│                                    │             │
│     ┌──────────────────────────────┼────────┐    │
│     │ MailProvider   AI(DeepSeek)  │Telegram│   │
│     │ Gmail/QQ IMAP  结构化提取     │Bot API │    │
│     └──────────────────────────────┴────────┘    │
│              SQLite（去重 + 推送历史）              │
└─────────────────────────────────────────────────┘
```

## 9. 数据模型（草案）

- `accounts`：账号配置（provider, 凭证加密引用, 规则, prompt, chat_id）
- `processed_emails`：`uid`, `message_id`, `account`, `status`, `summary_json`, `pushed_at`
  - 唯一键 `(account, uid)` 保证幂等

## 10. 迭代计划

| 里程碑 | 内容 | 验收 |
|--------|------|------|
| M0 | Electron + React 骨架 + Tailwind + UI 风格底座（Reroot 配色/组件） | 窗口启动、呈现风格 |
| M1 | QQ 邮箱 IMAP 接入，拉取并展示新邮件 | 界面看到真实邮件 |
| M2 | Gmail IMAP 接入 | 双邮箱统一接口 |
| M3 | AI 提取模块（DeepSeek） | 稳定输出结构化 JSON |
| M4 | Telegram 推送 + 去重 + 历史记录 UI | 端到端：收信→手机收到 |
| M5 | 设置页完善、过滤规则、失败重试、打包 `.app` | 生产可用 |

## 11. 已确认决策

| 事项 | 决策 |
|------|------|
| 形态 | **GUI 桌面客户端（macOS）** |
| 桌面框架 | **Electron**（+ React + TS + Tailwind + Vite，Node 后端） |
| AI 模型 | **DeepSeek**（OpenAI 兼容接口） |
| 提取字段 | 摘要 + 待办/行动项 + 关键实体（金额/日期/人名）+ 分类 + 紧急度 |
| 发送邮件 | **本期不做**，仅接收+推送 |
| UI 风格 | Reroot Healthcare（米白+橄榄绿+珊瑚橙 earth-tone） |

### 剩余待确认（均有默认值，不阻塞开工，后续可调）
1. **推送粒度**：默认全部推，规则作可选过滤
2. **Telegram 目标**：默认每账号一个 chat_id

---

### 下一步
确认桌面框架后，从 **M0** 开始：搭建 Electron + React 骨架 + Tailwind，先落地 Reroot 风格的 UI 底座（配色/组件/布局），再逐个里程碑推进。
